import { TitleText } from "../../common";

export const FavoritesTitle = () => {
  return (
    <div className='mb-6 flex items-center justify-between px-1'>
      <TitleText id='favorites-title' title='Favorites' />
    </div>
  );
};
