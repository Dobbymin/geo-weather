import { TitleText } from "../../common";

export const WeatherInformationTitle = () => {
  return (
    <div className='mb-6 flex items-center justify-between px-1'>
      <TitleText id='weather-information-title' title='현재 날씨' />
    </div>
  );
};
